/**
 * Project Model for SamadhanSetu (समाधानसेतु)
 * Responsibility: Project workspace schema, tasks checklist, and hardware BOM (Rule 4 compliant).
 */
import Workspace from './Workspace.js';

export default Workspace;
