/**
 * Users Route for SamadhanSetu (समाधानसेतु)
 * Responsibility: User registration, login, and stakeholder profiles (Rule 4 compliant).
 */
import authRouter from './auth.js';

export default authRouter;
